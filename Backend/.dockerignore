# Dependencies — reinstalled inside the image via npm ci
node_modules/

# Environment file — injected at runtime via docker-compose
.env

# Dev/debug files
scratch_verify.js
*.log

# Git
.git/
.gitignore
